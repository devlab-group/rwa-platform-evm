# Foundry build/test artifacts — regenerated by `forge build`/`forge test`.
out/
cache/

# forge script broadcast records (per-run tx logs) and their local key material.
broadcast/
cache/

# Installed dependencies — pinned via foundry.toml remappings, not vendored in-repo.
lib/

# .gas-snapshot is intentionally NOT ignored: it's the committed gas baseline
# (see `forge snapshot --diff`).
